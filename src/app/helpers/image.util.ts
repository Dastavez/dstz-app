export const generateImageSource = (src: string, variant?: string): string => {
    /* [TODO] Handle Cloudinary and Cloudfront URLs */
    switch (variant) {
        default:
            return src;
    }
};
